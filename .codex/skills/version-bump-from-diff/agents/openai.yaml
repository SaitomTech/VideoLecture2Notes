interface:
  display_name: "Version Bump from Diff"
  short_description: "差分から本アプリのバージョンを判断して更新"
  default_prompt: "Use $version-bump-from-diff to assess the VideoLecture2Notes changes, bump the app version, and when explicitly requested complete the commit, develop push, and main-based tag release flow."

policy:
  allow_implicit_invocation: true
