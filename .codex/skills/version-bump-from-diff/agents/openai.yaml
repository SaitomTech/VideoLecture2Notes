interface:
  display_name: "Version Bump from Diff"
  short_description: "差分から本アプリのバージョンを判断して更新"
  default_prompt: "Use $version-bump-from-diff to assess the VideoLecture2Notes diff from origin/main and bump the app version appropriately."

policy:
  allow_implicit_invocation: true
