interface:
  display_name: "Version Bump from Diff"
  short_description: "差分から本アプリのバージョンを判断して更新"
  default_prompt: "Use $version-bump-from-diff to assess the VideoLecture2Notes changes, bump the app version, commit and push to develop, then provide the GitHub GUI steps for merging main and creating the release tag."

policy:
  allow_implicit_invocation: true
