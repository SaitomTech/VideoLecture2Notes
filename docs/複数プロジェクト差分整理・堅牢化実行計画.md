# 複数プロジェクト差分整理・堅牢化 実行計画

## 1. 目的

現在の複数プロジェクト・複数動画・複数記事対応の差分を、初回リリース前に安全で変更しやすい状態へ整える。

この計画では、次の問題をまとめて解消する。

- プロジェクト保存形式と実行時状態の二重管理
- ストレージ初期化、作成、削除におけるデータ整合性の不足
- 過去に到達したステップへ戻る機能と、再生成時の無効化ルールの不足
- 動画追加UIの入れ子操作やダイアログのアクセシビリティ問題
- `ProjectDetailPage.tsx`への責務集中
- 一時サムネイルや失敗時ファイルの残留
- 現在参照されていない互換コード、型、フィールド
- 新しい保存・遷移仕様を保護する自動テストの不足

## 2. 前提

### 2.1 既存ユーザー互換は扱わない

現時点では既存ユーザーがいないため、旧形式のプロジェクトを新形式へ移行するマイグレーションは実装しない。

- 旧形式の読み込み互換を維持しない
- 旧形式専用の型、分岐、修復処理を残さない
- 開発中の旧プロジェクトはストレージバージョン変更時に一度だけ初期化してよい
- 初期化に失敗した場合は処理を中断し、部分的に消えた状態で続行しない
- 保存ファイルのバージョンが現行と異なる場合は、暗黙に読み替えず明示的に拒否する

### 2.2 実行中のデータ整合性は保証する

旧データ互換が不要でも、現行バージョンで次の失敗は発生し得る。

- ディスク容量不足
- ファイル権限エラー
- ffmpeg処理の失敗またはキャンセル
- アプリ終了やクラッシュ
- 同じ操作の連続実行

作成・更新・削除は、これらが起きてもプロジェクトJSONと実ファイルの参照関係を壊さないようにする。

### 2.3 UIの大幅な再設計はしない

現在の画面構成とユーザー導線は維持する。今回行うのは責務分割、状態管理の整理、不具合修正、アクセシビリティ改善であり、見た目の全面刷新ではない。

## 3. 完了条件

- 現行スキーマだけを完全に検証して読み書きできる
- ストレージ初期化が一度だけ、失敗を隠さず実行される
- 作成・削除・保存の途中失敗で、参照切れしたプロジェクトを作らない
- 到達済みステップには前後どちらにも移動できる
- 上流データを変更または再生成した場合、無効になった後続ステップへは直接移動できない
- 単なる閲覧やステップ移動では、到達済み情報を失わない
- プロジェクト、動画、記事、一時ファイルの削除失敗を適切に扱える
- 動画追加UIでFinderとYouTubeの操作が同時発火しない
- すべてのダイアログに名前、フォーカス管理、Escape操作がある
- `ProjectDetailPage.tsx`が機能単位のコンポーネントとhookへ分割される
- 宣言だけ残っている関数や、読み取られないフィールドが整理される
- ストレージ、ワークフロー、主要UI操作を自動テストで保護する
- `bunx tsc -b --pretty false`、`bun run lint`、`bun run fmt:check`、`git diff --check`が通る

## 4. 設計方針

### 4.1 永続モデルと画面用ワークスペースを分ける

現在の`MediaProject`は、永続化する`videos`・`articles`に加えて、選択中記事の`source`・`slides`・`workflow`などをルートにも複製している。この構造では、保存前の同期漏れや不要なrevision更新が起きやすい。

最終形は次の責務に分ける。

```text
PersistedProject
  id / title / videos / articles / activeArticleId / timestamps

Article
  inputMedia / settings / slides / outputs / workflow

ArticleWorkspace
  projectId / articleId / project metadata / active Article
  画面と各処理が参照する実行時ビュー
```

方針:

- 正規データは`PersistedProject.articles[]`内の`Article`だけに置く
- 選択中記事の内容をプロジェクトルートへ複製しない
- 既存の解析関数が一度に移行できない場合は、純粋な`toArticleWorkspace()`アダプターを一時的に使う
- アダプターは保存責務を持たず、逆同期関数も作らない
- 記事更新は`updateArticle(project, articleId, updater)`のような明示的な関数に集約する
- `contentRevision`などを残す場合は、実際に内容が変わったときだけ増やす

### 4.2 記事資産の保存先は引数で決める

モジュールグローバルな`activeArticleContext`は廃止する。

```ts
getSlideAssetPath(projectId, articleId, slideIndex)
getAudioAssetPath(projectId, articleId)
getRawTranscriptAssetPath(projectId, articleId)
```

- すべての資産APIへ`projectId`と`articleId`を明示的に渡す
- 非同期処理が画面遷移後も続いた場合に、別記事のディレクトリへ書かない構造にする
- パス組み立ては一箇所に集約し、各IDを検証する
- `runs/current`を使い続ける場合も、記事IDを含む完全なパスを関数内で組み立てる

### 4.3 「最後に見たステップ」と「到達可能なステップ」を分ける

`lastVisitedStep`だけでは、再開位置と成果物の有効性を同時に表現できない。

最低限、次の2つを分離する。

```ts
type ArticleWorkflow = {
  lastVisitedStep: ProjectStep
  maxReachedStep: ProjectStep
  lastOpenedAt: string
}
```

- `lastVisitedStep`: 次回開いたときの表示位置
- `maxReachedStep`: 現在の成果物で移動可能な最終ステップ
- ステップバーをクリックして過去へ移動しても`maxReachedStep`は変更しない
- 正規の「次へ」操作を完了したときだけ`maxReachedStep`を進める
- 上流処理を変更・再生成した場合は、影響範囲に応じて`maxReachedStep`を戻す

将来的には各成果物の入力fingerprintから到達可能性を導出できるが、今回の整理では既存のfingerprintを再利用しつつ、明示的な無効化関数を正規ルートにする。

```ts
advanceWorkflow(article, nextStep)
invalidateWorkflowAfter(article, changedStep)
```

### 4.4 再生成時の無効化ルール

初期ルールを次のように固定し、テストで表現する。

| 操作                             | 操作後に直接移動できる最終ステップ | 理由                                   |
| -------------------------------- | ---------------------------------- | -------------------------------------- |
| 過去ステップを閲覧するだけ       | 変更前の`maxReachedStep`           | 成果物は変わらない                     |
| スライド検出を再実行・境界を保存 | `generate-notes`                   | OCR、文字起こし、本文は再確認が必要    |
| OCRまたは文字起こしを再実行      | `generate-notes`                   | 本文生成が古くなる可能性がある         |
| 本文生成を完了                   | `article-review`                   | 新しい本文の確認が必要                 |
| 記事タイトル・本文・要約を保存   | `export`                           | Resultは表示時に最新内容から再生成する |
| Resultを開いて生成完了           | `export`                           | 全ステップが有効                       |

補足:

- モデル選択や設定変更だけでは無効化せず、その設定で処理を実行した時点で無効化する
- 同一fingerprintで処理がスキップされた場合は無効化しない
- 処理開始時ではなく、結果の保存に成功した時点で無効化する
- 処理失敗またはキャンセル時は、最後に正常保存された到達状態を維持する
- 複数スライドの一括処理中はステップ移動を無効化し、部分結果の状態を明示する

### 4.5 ファイル操作とJSON更新の整合性を揃える

#### 作成

1. 一時ディレクトリへ動画・記事資産を生成する
2. 生成結果を検証する
3. 正式ディレクトリへrenameする
4. 新しい参照を含むプロジェクトJSONを保存する
5. JSON保存に失敗した場合は、今回作成した正式ディレクトリだけを削除する

#### 削除

1. 対象ディレクトリをプロジェクト内の`.trash/<operationId>`へrenameする
2. 対象を除いたプロジェクトJSONを保存する
3. JSON保存に失敗した場合は、対象ディレクトリを元の場所へ戻す
4. JSON保存に成功した場合は、trashを削除する
5. trash削除だけが失敗した場合は、プロジェクト自体は成功扱いとし、次回起動時に再回収する

原則:

- 必須処理では`.catch(() => undefined)`を使わない
- 失敗を無視してよい処理は、関数名を`bestEffort...`にするか呼び出し側へ理由を書く
- JSON保存前後の資産操作はrepository層へ集約し、`App.tsx`から直接呼ばない
- 同一プロジェクトへの保存はキュー化し、古い非同期結果が新しい状態を上書きしないようにする

## 5. 実行フェーズ

### Phase 0: 差分と開発データの固定

- [x] 現在必要なuntrackedファイルを一覧化する
- [x] `src/features/project/`、`projectMedia.ts`、`videoRange.ts`、`YoutubeDownloadModal.tsx`をコミット対象として明示する
- [x] 旧ページ削除が新画面へ完全に置換されていることを確認する
- [x] 開発用プロジェクトデータは初期化されることをREADMEまたは開発メモへ記載する
- [ ] 作業前に代表的な手動確認用動画を決める

成果物:

- `src/features/project/`配下の詳細画面・動画import・記事作成・削除ダイアログ
- `src/lib/project/projectMedia.ts`、`src/lib/project/videoRange.ts`、`src/lib/ui/useDialogA11y.ts`
- `src/features/import/components/YoutubeDownloadModal.tsx`、`tests/project-workflow.test.ts`
- 現在差分のスコープ表
- 手動確認シナリオ

### Phase 1: 現行ストレージ形式の一本化

対象:

- `src/types/project.ts`
- `src/schemas/project.ts`
- `src/lib/storage/projectStorage.ts`
- `src/lib/project/project.ts`

作業:

- [x] `PROJECT_VERSION`とストレージレイアウトバージョンの役割を明文化する
- [x] 旧形式移行コードを追加せず、現行以外のversionを拒否する
- [x] `videos`、`articles`、workflow、media、解析結果を含む完全なZodスキーマを定義する
- [x] `z.unknown()`からの二重キャストを廃止する
- [x] ストレージ初期化では、バージョン不一致時だけ全削除する
- [x] 全削除の失敗を握り潰さず、markerは初期化完了後にだけ書く
- [x] `storageInitialization`が失敗した場合に、再試行可能な状態へ戻す
- [x] `.tmp`、`.bak`、`.trash`の起動時回収ルールを定義する
- [x] `PersistedProject`と画面用workspace型の分離を開始する

テスト:

- [ ] 空ストレージの初期化
- [ ] 同じversionで既存データを消さない
- [ ] 異なるversionで一度だけ初期化する
- [ ] 初期化失敗時にmarkerを書かない
- [x] 不正なarticle/videoを読み込み時に拒否する
- [x] 現行より新しいversionを拒否する
- [ ] atomic write失敗時に直前のJSONを復旧する

### Phase 2: ワークフロー到達状態と無効化

対象:

- `src/lib/workflow.ts`
- `src/components/WorkflowBar.tsx`
- `src/lib/project/project.ts`
- `src/app/App.tsx`
- 各処理完了コールバック

作業:

- [x] `lastVisitedStep`と`maxReachedStep`を分離する
- [x] ステップ比較・進行・無効化を純粋関数へ集約する
- [x] `App.tsx`の画面遷移ガードを共通関数へ置き換える
- [x] 到達済みの前後移動では永続成果物を変更しない
- [x] スライド再検出、OCR、文字起こし、本文再生成に無効化処理を接続する
- [x] 同一fingerprintで何も変わらなかった処理は到達状態を維持する
- [x] Article Previewの未保存編集がある場合はステップ移動を止める
- [x] Resultは常に保存済みの最新記事から生成する

テスト:

- [x] 04到達後に02へ戻り、変更なしで04へ直接戻れる
- [x] 04到達後に01を再実行すると03・04へ直接移動できない
- [x] 04到達後にOCRまたは文字起こしを再実行すると04へ直接移動できない
- [x] 本文生成完了後は03へ移動できる
- [x] 記事編集後に04を開くと最新内容で再生成される
- [ ] 失敗・キャンセルでは直前の有効な到達状態を保持する
- [x] アプリ再起動後も`lastVisitedStep`と`maxReachedStep`が正しく復元される

### Phase 3: プロジェクト・動画・記事のファイル操作をtransaction化

対象:

- `src/lib/storage/projectStorage.ts`
- `src/lib/storage/projectAssets.ts`
- `src/lib/project/projectMedia.ts`
- `src/app/App.tsx`

作業:

- [x] `ProjectRepository`相当の層に作成・保存・削除をまとめる
- [x] 動画追加失敗時にコピー済み資産を必ず回収する
- [x] 記事作成後のJSON保存失敗時に生成済み記事資産を回収する
- [x] 動画・記事削除をtrashへのrenameで復元可能にする
- [x] 動画削除後も記事側の独立コピーが利用できることを保証する
- [x] プロジェクト削除の失敗をダイアログへ返す
- [x] 削除成功後のtrash回収を起動時にも実行する
- [x] サムネイル生成失敗時は存在しない`thumbnailPath`を保存しない
- [x] 同一プロジェクトへの保存を直列化する

テスト:

- [ ] 動画コピー成功・JSON保存失敗で孤立ディレクトリを残さない
- [ ] 記事生成成功・JSON保存失敗で孤立ディレクトリを残さない
- [ ] trash移動後のJSON保存失敗で元へ戻る
- [ ] trash削除失敗でもプロジェクト一覧と再起動が壊れない
- [ ] サムネイル生成失敗時にフォールバック表示になる
- [ ] 連続保存で新しい状態が古い状態に上書きされない

### Phase 4: 資産APIからグローバル状態を除去

対象:

- `src/lib/storage/projectAssets.ts`
- スライド検出、OCR、文字起こし、本文生成、exportの呼び出し側

作業:

- [x] `setActiveArticleAssetContext`を廃止する
- [x] 資産APIへ`articleId`を明示的に渡す
- [x] article用、project video用、一時処理用のパスを分ける
- [x] パスへ埋め込むproject/article/video IDを検証する
- [x] `runs/current`の上書き条件を整理する
- [x] 画面遷移中に完了した非同期処理が元の記事へ保存される構造にする

テスト:

- [ ] 2記事を交互に開いても資産が混ざらない
- [ ] 処理中にプロジェクト画面へ戻っても保存先が変わらない
- [ ] キャンセル後に別記事を処理しても前記事のパスを使わない

### Phase 5: 動画追加UIとダイアログの修正

対象:

- `src/features/import/components/VideoDropZone.tsx`
- `src/features/import/components/YoutubeDownloadModal.tsx`
- `src/features/project/VideoImportPanel.tsx`
- `src/features/home/HomePage.tsx`
- プロジェクト詳細内の各ダイアログ

作業:

- [x] ドロップ領域を非ボタン化し、Finder・YouTubeを独立したbuttonにする
- [x] Enter/SpaceでFinderとYouTubeが同時発火しないことを保証する
- [x] 共通`Modal`または`ConfirmDialog`を用意する
- [x] `aria-labelledby`または`aria-label`を全ダイアログへ設定する
- [x] Escape、背景クリック、初期フォーカス、フォーカス復帰を共通化する
- [x] 処理中は閉じられないルールを統一する
- [x] Homeのプロジェクト読み込み中表示と二重クリック防止を実装する
- [x] 複数のopen要求が競合した場合、最新要求だけを反映するか操作自体を直列化する

確認:

- [ ] マウスなしで動画追加、キャンセル、確認操作ができる
- [ ] VoiceOverで各ダイアログ名と閉じるボタンを判別できる
- [ ] Finder選択中にYouTubeモーダルが開かない
- [ ] プロジェクトを連続クリックしても別プロジェクトが後から表示されない

### Phase 6: ProjectDetailPageの分割

分割案:

```text
src/features/project/
  ProjectDetailPage.tsx
  components/
    ProjectHeader.tsx
    ProjectTabs.tsx
    ArticleList.tsx
    VideoList.tsx
    SelectArticleVideoDialog.tsx
    DeleteProjectDialog.tsx
    DeleteArticleDialog.tsx
    DeleteVideoDialog.tsx
  article-creator/
    CreateArticleDialog.tsx
    RangeEditor.tsx
    RangeList.tsx
    CropEditor.tsx
    PerspectiveCropEditor.tsx
    VideoTimeline.tsx
    useArticleRangeEditor.ts
    useRangeThumbnails.ts
    rangeDraft.ts
```

作業:

- [x] `ProjectDetailPage`を画面構成とダイアログ制御だけにする
- [x] 時刻変換、範囲検証、crop変換を純粋関数へ移す
- [x] 範囲編集stateを`useArticleRangeEditor`へ集約する
- [x] サムネイル生成のdebounce、キャンセル、後片付けを`useRangeThumbnails`へ集約する
- [x] 作成成功・キャンセルのどちらでも一時サムネイルを削除する
- [x] 削除確認ダイアログの重複を共通化する
- [x] IME対応をタイトル・区間タイトルの入力処理へまとめる
- [x] コンポーネント間では保存済みデータとdraftを明確に分ける

目安:

- `ProjectDetailPage.tsx`: 300行以下
- `CreateArticleDialog.tsx`: 250行以下
- hookまたは純粋関数: 1責務を説明できる単位
- 見た目だけを分割し、同じ巨大なpropsを横流しする分割は避ける

### Phase 7: 不要コード・不要データの削除

候補は参照検索とテスト通過を確認してから削除する。

- [x] `createMediaProject`
- [x] `updateProjectSource`
- [x] `loadProjectForResume`と`ProjectOpenResult`
- [x] `ensureProjectVideoPath`
- [x] `prepareArticleRunDirectory`
- [x] 旧`derived`・`trimmed`資産用API
- [x] `removeProjectAnalysisAssets`
- [x] 読み取られていない`contentRevision`、`inputRevision`、video revision/snapshot系フィールド
- [x] 旧ページ削除後に残ったimport、型、Tauri capability
- [x] 開発生成物、`.DS_Store`、不要な画像出力

判断基準:

- 近い将来使う可能性だけでは残さない
- 永続フィールドは利用箇所と更新規則を説明できるものだけ残す
- Tauri capabilityは実際に必要な最小パス・操作へ絞る
- 削除後に`rg`で参照がないことと、ビルドが通ることを確認する

### Phase 8: テスト基盤と回帰確認

現在は専用のテストスクリプトがないため、まず純粋ロジックとrepositoryを対象に小さく導入する。

推奨:

- 純粋関数・repository: Bun testまたはVitest
- React操作: Vitest + Testing Library + jsdom
- Tauriファイル操作: filesystem adapterを差し替えたテスト
- 実動画・ffmpeg: 少数のfixtureを使うintegration test
- UI全体: リリース前の手動スモークテスト

追加するスクリプト例:

```json
{
  "test": "bun test",
  "test:watch": "bun test --watch"
}
```

最低限必要なテスト群:

- [x] workflowの進行・戻る・再生成時無効化
- [ ] project/articleの更新が対象記事だけに適用される
- [x] 完全なproject schemaの正常・異常ケース
- [ ] storage version初期化
- [ ] atomic writeとtrash rollback
- [ ] 動画・記事作成失敗時のcleanup
- [x] range正規化と境界値
- [ ] VideoDropZoneのFinder・YouTube排他動作
- [ ] ダイアログの名前、Escape、フォーカス復帰
- [ ] Homeの連続open防止

## 6. コミット分割

一つの巨大コミットにせず、次の単位で進める。

1. `test: add project workflow and storage test harness`
2. `refactor: define strict current project schema`
3. `fix: make project asset mutations recoverable`
4. `fix: track reached workflow separately from navigation`
5. `refactor: pass article identity explicitly to asset APIs`
6. `fix: separate video drop zone actions and accessible dialogs`
7. `refactor: split project detail and article range editor`
8. `chore: remove obsolete project compatibility code`

各コミットで型チェック、lint、関連テストを通す。構造整理と振る舞い変更は可能な限り同じコミットへ混ぜない。

## 7. 実装順序と依存関係

```text
テスト基盤
  ↓
現行スキーマの固定
  ↓
workflow状態の分離 ─────────┐
  ↓                         │
repositoryと削除transaction │
  ↓                         │
asset APIの明示化            │
  └──────────────┬──────────┘
                 ↓
        UI不具合とDialog共通化
                 ↓
        ProjectDetailPage分割
                 ↓
          不要コードの削除
                 ↓
          全体回帰確認
```

正規モデルや関数境界が定まる前に`ProjectDetailPage`を細かく分割すると、古いprops構造を多数のファイルへ拡散させる。そのため、データ・workflow・repositoryを先に整理する。

## 8. 手動スモークテスト

### プロジェクト

- [ ] 新規プロジェクト作成
- [ ] 名前変更
- [ ] 一覧へ戻る、再度開く
- [ ] 詳細画面から削除
- [ ] 削除失敗時にエラーが表示され、データが残る

### 動画

- [ ] Finderから追加
- [ ] ドラッグ&ドロップで追加
- [ ] YouTubeから追加
- [ ] サムネイル生成失敗時のフォールバック
- [ ] 動画削除後も作成済み記事を開ける

### 記事作成

- [ ] 1区間で作成
- [ ] 複数区間で作成
- [ ] 長方形crop
- [ ] 四隅補正
- [ ] 自動cropの成功・失敗・キャンセル
- [ ] 作成ダイアログのキャンセル後に一時ファイルが残らない

### ステップ

- [ ] 01から04まで完了
- [ ] 04から02へ戻り、変更なしで04へ戻る
- [ ] 01再実行後に無効な03・04へ移動できない
- [ ] 02再生成後に03で最新結果を確認できる
- [ ] 03編集後に04が最新内容を生成する
- [ ] 各処理の実行中、失敗、キャンセル時の移動制御

### 再起動

- [ ] プロジェクト一覧と件数が復元される
- [ ] active articleと最後に見たステップが復元される
- [ ] 到達可能な最終ステップが復元される
- [ ] `.tmp`、`.bak`、`.trash`が回収される

## 9. リリース前チェック

```bash
bunx tsc -b --pretty false
bun run lint
bun run fmt:check
git diff --check
bun test
bunx react-doctor@latest . --verbose --scope changed
```

- [ ] 必須の新規ファイルがuntrackedのまま残っていない
- [ ] 意図しない画像、動画、`.DS_Store`を含めていない
- [ ] Tauri capabilityが必要最小限になっている
- [ ] 開発用旧ストレージの初期化方針が明記されている
- [ ] 手動スモークテストを完了している
- [ ] 重大度P0/P1の既知問題が残っていない

## 10. 今回行わないこと

- 旧プロジェクト形式からのデータ移行
- 複数ユーザー・クラウド同期
- 複数ウィンドウでの同時編集
- undo可能なユーザー向けゴミ箱UI
- UIデザインの全面刷新
- 未使用フィールドを想定用途だけで残すこと

## 11. 推奨する最初の着手単位

最初は次の3点を一つの小さな作業単位として実施する。

1. workflowの純粋関数とテストを追加する
2. `lastVisitedStep`と`maxReachedStep`を分離する
3. 変更なしの再訪と、再生成後の無効化をテストで固定する

この単位はファイル削除や大規模コンポーネント分割を伴わず、直近で変更したステップバーの仕様を先に安定させられる。その後、ストレージschemaとtransaction化を独立した作業として進める。

## 12. 実装結果

この計画に基づく実装は、初回リリース前の現行データだけを対象として完了した。

- プロジェクト一覧から削除操作を外し、プロジェクト詳細の最右端にアイコンのみの削除ボタンを配置した。
- 記事作成フローの各ページにあった「戻る」導線を削除し、ステップバーと画面上部の共通導線へ整理した。
- `lastVisitedStep`と`maxReachedStep`を分離し、到達済みステップへの直接移動と、再生成時の後続無効化を実装した。
- 現行project schema、ストレージ初期化、atomic write、asset trash、作成・削除時のcleanupを実装した。旧形式のmigrationは実装していない。
- 記事資産APIから暗黙のactive article状態を除去し、`articleId`を明示的に渡す形へ統一した。
- 動画追加UI、ダイアログのフォーカス・Escape・背景クリック、Homeの連続open競合、ProjectDetailと記事作成ダイアログの責務分割を実装した。
- `tests/project-workflow.test.ts`に、workflow、再起動相当のserialize/parse、OCR、文字起こし、本文生成、schema、range境界のテストを追加した。

### 12.1 未実施・リリース前に確認する項目

次の項目は実動画とTauri実行環境が必要なため、コード検証とは分けて残している。

- [ ] Tauri filesystem adapterを差し替えたstorage transactionテスト
- [ ] VideoDropZone、ダイアログ、Homeの連続openのUI自動テスト
- [ ] 実動画を使ったFinder、YouTube、crop、ffmpeg、キャンセルの手動スモークテスト
- [ ] アプリ再起動による`.tmp`、`.bak`、`.trash`回収の手動確認

実行済みのローカル検証:

```text
11 tests passed / 46 expectations
bunx tsc -b --pretty false
bun run lint
bun run fmt:check
git diff --check
```

`react-doctor`は変更ファイルに対して実行済み（74/100、5件）。残る指摘は既存ページの複雑度4件と、長い動画でsidecarを同時起動しないため意図的に直列化しているffmpeg処理1件であり、今回の機能不具合とは切り分けている。schema factoryの指摘は`z.strictObject`への統一で解消した。
