# Video Notes — 実装仕様書

## 1. プロダクト概要

### 1.1 アプリ名

**Video Notes**

### 1.2 目的

カンファレンス、オンライン授業、大学講義、ウェビナー、社内勉強会などの動画を入力し、動画内のスライド切り替わりを検出して、各スライド画像と対応する発話テキストを並べた「記事形式のノート」を完全ローカルで生成するデスクトップアプリケーション。

最終成果物の中心は文字起こし単体ではなく、以下のような構造を持つ読み物。

```text
Title

[Slide Image]

Section text...

[Slide Image]

Section text...

[Slide Image]

Section text...
```

動画を見返さなくても、スライドと発話内容を記事として追える状態を作る。

### 1.3 想定ユースケース

- 技術カンファレンス
- セミナー
- ウェビナー
- オンライン授業
- 大学講義
- プレゼンテーション録画
- 社内勉強会
- 製品デモ
- スライド共有を伴う会議録画

### 1.4 最重要要件

- 動画データをクラウドへ送信しない
- OpenAI API等の外部AI APIを使わない
- OCR、文字起こし、文章補正をすべてローカルで実行する
- 一度必要モデルを取得した後は完全オフラインで処理可能
- M1 Mac / 16GB Unified Memoryで実用的に動作する
- ユーザーがffmpeg / whisper.cpp / llama.cppを個別インストールしなくてよい
- clone後にほぼ自動で開発環境を立ち上げられる
- 最終成果物としてHTML記事を生成できる

---

# 2. 最終成果物

## 2.1 Primary Output

**HTML記事**

Video Notesの主要成果物は、ブラウザで単体閲覧できるHTMLファイルとする。

例:

```html
<article>
  <h1>Session Title</h1>

  <section>
    <img src="./assets/slide-001.jpg" />
    <p>
      このセクションではローカルLLMの基本構成について説明しています。
      ...
    </p>
  </section>

  <section>
    <img src="./assets/slide-002.jpg" />
    <p>
      次にWhisperを利用した文字起こしについて説明します。
      ...
    </p>
  </section>
</article>
```

## 2.2 Export Directory

```text
video-notes-export/
├─ index.html
├─ assets/
│  ├─ slide-001.jpg
│  ├─ slide-002.jpg
│  └─ slide-003.jpg
├─ notes.md
├─ transcript.txt
├─ transcript.srt
└─ project.json
```

HTMLは外部CDNや外部JSに依存しない。

```text
index.html
+
local assets
```

だけで開けるようにする。

## 2.3 Secondary Outputs

- Markdown
- JSON
- TXT
- SRT
- 将来: VTT / PDF

---

# 3. 出力記事の構造

記事は原則として「1スライド = 1セクション」とする。

```ts
type ArticleSection = {
  id: string;

  startMs: number;
  endMs: number;

  imagePath: string;

  ocrText: string;

  transcriptRaw: string;
  transcriptCorrected: string;

  body: string;
};
```

HTML生成時には以下を使用する。

```text
representative slide image
+
article body
```

OCR raw textとraw transcriptは通常表示しないが、必要に応じて詳細表示可能にする。

---

# 4. 記事本文生成方針

## 4.1 基本

記事本文 `body` は、補正済み文字起こしをベースに生成する。

初期バージョンでは過度な要約を避ける。

目的:

```text
spoken transcript
↓
読みやすい文章
```

であり、

```text
spoken transcript
↓
短い要約
```

ではない。

## 4.2 Pass分離

LLM処理は最低2段階に分ける。

### Pass 1: Transcript Correction

OCR情報を使ってWhisper誤認識を訂正。

### Pass 2: Article Formatting

補正済みTranscriptを記事として読みやすく整形。

この2処理を混ぜない。

## 4.3 Article Formatting Rules

許可:

- フィラー除去
- 句読点追加
- 適切な段落分け
- 明らかな言い直しの整理
- 重複表現の軽微な整理

禁止:

- 発話していない事実の追加
- 大幅な要約
- 主張の変更
- スライドだけを根拠にした文章追加

---

# 5. 対象環境

初期リファレンス環境:

```text
Apple Silicon Mac
M1
16GB Unified Memory
macOS
arm64
```

v1まではmacOS arm64を優先する。

将来的に:

- Windows x64
- Linux x64
- 他Apple Silicon

を追加可能にする。

---

# 6. 技術スタック

## Desktop

- Tauri 2
- React
- TypeScript
- Vite

## State

初期段階では外部state managerを導入しない。

使用:

- `useState`
- `useReducer`
- React Context

必要性が明確になった時点でのみ外部state manager導入を再検討する。

## Validation

- Zod

## Media

- ffmpeg
- ffprobe

## Speech-to-Text

- whisper.cpp
- Whisper large-v3-turbo Q5

## OCR / Vision

- llama.cpp
- 第一候補: GLM-OCR GGUF
- 代替候補: PaddleOCR-VL GGUF

## Transcript Correction / Article Formatting

- llama.cpp
- 3B〜4B Instruct GGUF
- Q4 / Q5量子化

## Package Manager / Script Runtime

- Bun

Node.js + pnpmは前提にしない。

開発用TypeScript scriptもBunで直接実行する。

例:

```bash
bun run setup
bun run prepare:sidecars
```

package scripts経由では:

```bash
bun run setup
bun tauri dev
```

---

# 7. Architecture

```text
                         Video Notes
                            │
                            ▼
                     Tauri + React
                        TypeScript
                            │
       ┌────────────────────┼────────────────────┐
       │                    │                    │
       ▼                    ▼                    ▼
    ffmpeg              whisper.cpp          llama.cpp
       │                    │                    │
       │                    │           ┌────────┴────────┐
       │                    │           ▼                 ▼
       │                    │        OCR/VLM           Text LLM
       │                    │           │                 │
       └────────────────────┴───────────┴─────────────────┘
                            │
                            ▼
                      MediaProject
                            │
                            ▼
                    Article Generator
                            │
               ┌────────────┼────────────┐
               ▼            ▼            ▼
             HTML       Markdown        JSON
```

---

# 8. TypeScript / Rust責務

## TypeScript

- GUI
- Project state
- Pipeline orchestration
- Job management
- Slide detection
- Hash computation
- Result parsing
- llama-server HTTP client
- Prompt construction
- Article generation
- HTML generation
- Markdown generation
- Export
- Model metadata
- Setup scripts

## Rust

極薄に保つ。

- Tauri commands
- sidecar permissions
- sidecar process spawn
- filesystem integration
- OS integration
- process lifecycle

AIロジックはRust側へ実装しない。

---

# 9. 処理パイプライン

```text
input.mp4
   │
   ▼
ffprobe
   │
   ▼
Crop UI
   │
   ▼
slide region
   │
   ▼
frame sampling
   │
   ▼
perceptual hash
   │
   ▼
slide boundaries
   │
   ▼
manual review
   │
   ├─────────────────────────┐
   │                         │
   ▼                         ▼
segment audio            slide image
   │                         │
   ▼                         ▼
whisper.cpp              OCR VLM
   │                         │
   ▼                         ▼
raw transcript            OCR text
   │                         │
   └────────────┬────────────┘
                ▼
       transcript correction
                │
                ▼
       corrected transcript
                │
                ▼
         article formatting
                │
                ▼
          ArticleSection[]
                │
        ┌───────┼────────┐
        ▼       ▼        ▼
      HTML      MD      JSON
```

---

# 10. 動画読み込み

## 対応形式

最低限:

- mp4
- mov
- m4v
- mkv
- webm

## ffprobe metadata

```ts
type MediaMetadata = {
  path: string;
  durationMs: number;
  width: number;
  height: number;
  fps?: number;
  videoCodec?: string;
  audioCodec?: string;
};
```

---

# 11. スライド領域Crop

動画プレビュー上にCrop rectangleを表示する。

操作:

- Drag
- Resize
- Reset
- Full frame
- Apply

```ts
type CropRegion = {
  x: number;
  y: number;
  width: number;
  height: number;
};
```

座標は元動画pixel基準。

## Cropの扱い

通常はcropped動画そのものを作らず、後続ffmpeg処理へcrop filterを適用する。

デバッグ用途としてcropped preview video生成を可能にする。

---

# 12. スライド切り替わり検出

## 方針

LLMは使用しない。

```text
video
↓
crop
↓
low resolution sampling
↓
grayscale
↓
perceptual hash
↓
hamming distance
↓
boundary detection
```

## Sampling

初期値:

```ts
const SAMPLE_INTERVAL_MS = 500;
```

約2fps。

解析解像度目安:

```text
320x180
```

## Hash

v0.1:

- dHash

将来:

- pHash

```ts
type FrameHash = {
  timestampMs: number;
  hash: string;
};
```

## Boundary

```ts
type SlideBoundary = {
  id: string;
  timestampMs: number;
  distance: number;
  source: "auto" | "manual";
};
```

単一差分だけで境界確定しない。

```text
threshold超過
↓
candidate
↓
一定時間状態確認
↓
安定
↓
boundary
```

---

# 13. Boundary Review UI

検出結果は必ずユーザーが確認・修正可能にする。

必要機能:

- boundary drag
- Add boundary
- Delete boundary
- previous/next frame preview
- threshold変更
- 再検出

Timeline例:

```text
00:00       03:24          07:10
|------------|---------------|
             ↑
          draggable
```

自動検出精度100%を目標にしない。

---

# 14. Slide Segment

```ts
type SlideSegment = {
  id: string;
  index: number;
  startMs: number;
  endMs: number;
};
```

動画を物理的に全segmentへ分割することは必須ではない。

timestampベースで管理し、必要時のみffmpegで部分処理する。

---

# 15. Representative Slide Image

各Slide Segmentにつき1枚を記事用画像として生成する。

初期方式:

```text
segment midpoint
```

ただしtransitionやanimation途中を避ける。

将来的には:

```text
複数候補frame
↓
hash stability
↓
best stable frame
```

で代表画像を決める。

記事HTMLで使用するため、代表画像はexport assetsへコピーする。

---

# 16. Whisper文字起こし

## Runtime

whisper.cpp

## Default Model

```text
Whisper large-v3-turbo Q5
```

## Audio Input

ffmpegで各区間を:

```text
16kHz
mono
PCM WAV
```

へ変換する。

## Model

```ts
type TranscriptSegment = {
  startMs: number;
  endMs: number;
  text: string;
};

type RawTranscript = {
  language?: string;
  text: string;
  segments: TranscriptSegment[];
};
```

timestampは最終的に元動画絶対時間へ変換する。

---

# 17. OCR

## Runtime

llama.cpp / llama-server

## Default

```text
GLM-OCR GGUF
```

代替:

```text
PaddleOCR-VL GGUF
```

## Input

representative slide image。

## Output

```ts
type SlideOcrResult = {
  rawText: string;
  title?: string | null;
  terms?: string[];
};
```

OCR時は要約させない。

Prompt方針:

```text
画像に含まれる文字を忠実に抽出する。
要約しない。
推測で情報を追加しない。
見えている文字のみを返す。
```

---

# 18. Transcript Correction

## Input

```text
raw Whisper transcript
+
slide OCR
+
reference terms
```

## Purpose

修正対象:

- 固有名詞
- 製品名
- 技術用語
- 英単語
- API名
- 人名
- 数値
- 略語

## 禁止

- 要約
- 情報追加
- 発話削除
- 意味変更
- OCRに根拠のない推測

## Audit

```ts
type Correction = {
  before: string;
  after: string;
  reason?: string;
};

type CorrectedTranscript = {
  raw: string;
  corrected: string;
  corrections: Correction[];
};
```

必ずraw transcriptを保存する。

---

# 19. Article Formatting

Transcript Correctionの後に別のLLM passとして実行する。

```ts
type ArticleFormattingResult = {
  body: string;
};
```

Prompt方針:

```text
補正済み文字起こしを、内容を変えずに読みやすい記事本文へ整形する。

許可:
- フィラー除去
- 句読点
- 段落分け
- 軽微な重複整理

禁止:
- 要約
- 発言していない内容の追加
- 意味変更
```

ユーザー設定として将来的に以下を追加可能。

```ts
type ArticleDetailLevel =
  | "verbatim"
  | "clean"
  | "concise";
```

初期デフォルト:

```text
clean
```

---

# 20. Article Data Model

```ts
type ArticleSection = {
  id: string;

  slideIndex: number;

  startMs: number;
  endMs: number;

  imagePath: string;

  ocr: {
    rawText: string;
    terms: string[];
  };

  transcript: {
    raw: string;
    corrected: string;
    corrections: Correction[];
  };

  body: string;
};
```

```ts
type VideoNotesArticle = {
  title: string;
  sourceVideo: string;
  durationMs: number;
  sections: ArticleSection[];
};
```

---

# 21. HTML Generator

## 21.1 Requirement

生成HTMLは単体配布できること。

外部依存:

```text
なし
```

禁止:

- CDN CSS
- CDN JS
- remote fonts
- remote images
- external analytics

## 21.2 HTML Structure

```html
<!doctype html>
<html lang="ja">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>...</title>
  <style>
    /* inline local CSS */
  </style>
</head>

<body>
  <main class="article">
    <header>
      <h1>...</h1>
    </header>

    <section class="slide-section">
      <figure>
        <img src="./assets/slide-001.jpg">
      </figure>

      <div class="content">
        ...
      </div>
    </section>
  </main>
</body>
</html>
```

## 21.3 Default HTML Design

読み物として見やすいことを優先。

- max-width: 約900〜1100px
- responsive
- slide imageを本文幅に合わせる
- bodyは16〜18px程度
- dark modeは将来
- print stylesheetを用意してもよい

## 21.4 Timestamp

各セクションに元動画timestampを表示可能にする。

```text
03:24 — 07:10
```

将来的に元動画URLを持つ場合はtimestamp link化可能だが、v1ではローカル動画が主対象。

---

# 22. Markdown Generator

HTMLと同じArticleSectionをソースとする。

例:

```md
# Session Title

## 00:00–03:24

![Slide 1](./assets/slide-001.jpg)

本文...

## 03:24–07:10

![Slide 2](./assets/slide-002.jpg)

本文...
```

HTMLとMarkdownで別々のAI生成をしない。

同じArticle modelからdeterministicにrenderする。

---

# 23. Project Data Model

```ts
type MediaProject = {
  version: number;

  id: string;

  source: MediaSource;

  crop: CropRegion;

  settings: ProjectSettings;

  slides: SlideData[];

  article?: VideoNotesArticle;

  createdAt: string;
  updatedAt: string;
};
```

```ts
type SlideData = {
  id: string;
  index: number;

  startMs: number;
  endMs: number;

  detection: {
    source: "auto" | "manual";
    hash?: string;
    distance?: number;
  };

  image: {
    representativeFramePath?: string;
  };

  ocr?: {
    rawText: string;
    title?: string | null;
    terms?: string[];
    model: string;
  };

  transcript?: {
    raw: string;
    corrected?: string;
    articleBody?: string;
    segments: TranscriptSegment[];
    corrections?: Correction[];
    model: string;
  };
};
```

---

# 24. Storage

Project directory:

```text
project/
├─ project.json
├─ cache/
│  ├─ audio/
│  ├─ frames/
│  ├─ hashes/
│  ├─ ocr/
│  └─ llm/
└─ export/
```

`project.json`をcanonical stateとする。

再実行時はcacheを可能な限り利用する。

---

# 25. Cache Policy

以下をstage単位で再利用可能にする。

```text
source unchanged
+
crop unchanged
→ frame/hash再利用

boundary unchanged
→ audio/transcript再利用

image unchanged
→ OCR再利用

transcript/OCR unchanged
→ correction再利用

corrected transcript unchanged
→ article formatting再利用
```

各cacheにはinput hashを持たせる。

---

# 26. Model Manager

## M1 16GB方針

モデルを同時常駐させない。

```text
Whisper
load
↓
all transcription
↓
unload

OCR
load
↓
all OCR
↓
unload

Text LLM
load
↓
all correction
↓
article formatting
↓
unload
```

Text LLMはCorrectionとArticle Formattingで共有してよい。

---

# 27. Sidecars

必要:

```text
ffmpeg
ffprobe
whisper-cli
llama-server
```

Tauri sidecarとして扱う。

ユーザーにHomebrew installを要求しない。

---

# 28. Setup / Developer Experience

目標:

```bash
git clone <repo>
cd video-notes
bun install
bun run dev
```

これだけで開発可能。

## dev script

内部的に:

```text
bun run dev
↓
Bun scriptでsidecar確認
↓
不足sidecarをdownload
↓
SHA-256検証
↓
Tauri dev起動
```

モデルはサイズが大きいため、開発時に自動downloadするかは設定可能にする。

```bash
bun run setup
```

でまとめて取得できるようにする。

---

# 29. Download Manifest

```ts
type DownloadArtifact = {
  id: string;
  version: string;

  platform: string;
  arch: string;

  url: string;
  sha256: string;

  destination: string;
};
```

versionは必ずpinする。

---

# 30. Model Distribution

モデルはGitへ含めない。

macOS保存先:

```text
~/Library/Application Support/Video Notes/models/
```

例:

```text
models/
├─ whisper/
│  └─ large-v3-turbo-q5_0.bin
├─ ocr/
│  └─ glm-ocr/
└─ text/
   └─ correction.gguf
```

初回起動時にModel Managerでdownload可能にする。

---

# 31. llama-server

OCR / Text LLMはllama-server経由。

```text
Tauri
↓
spawn
llama-server
↓
127.0.0.1:<dynamic-port>
```

localhostのみ。

外部interfaceへbindしない。

Lifecycle:

```text
spawn
↓
healthcheck
↓
process batch
↓
terminate
↓
memory release
```

---

# 32. Job Manager

```ts
type JobStage =
  | "probe"
  | "detect-slides"
  | "extract-audio"
  | "transcribe"
  | "ocr"
  | "correct"
  | "format-article"
  | "export";

type JobStatus =
  | "queued"
  | "running"
  | "cancelled"
  | "failed"
  | "completed";

type ProcessingJob = {
  id: string;
  stage: JobStage;
  status: JobStatus;
  progress: number;
  message?: string;
};
```

必要:

- progress
- cancel
- retry
- resume
- stage単位再実行

---

# 33. GUI Flow

```text
Import
↓
Crop
↓
Detect Slides
↓
Review Boundaries
↓
Generate Notes
↓
Review Article
↓
Export HTML
```

---

# 34. Screen 1 — Import / Crop

- Drag & Drop
- File picker
- Video preview
- Crop rectangle
- Metadata
- Save crop preset
- Continue

---

# 35. Screen 2 — Slide Detection

- Sample interval
- Hash threshold
- Detect
- Timeline
- Slide thumbnails
- Add/Delete boundary
- Drag boundary
- Re-detect
- Continue

---

# 36. Screen 3 — Processing

表示例:

```text
Generating Video Notes

✓ Preparing media
✓ Detecting slides
✓ Transcribing
● OCR            12 / 38
○ Correcting transcript
○ Formatting article
```

処理中でも完了済みスライドを閲覧できるようにしてよい。

---

# 37. Screen 4 — Article Review

最終成果物に近いWYSIWYG的レビュー画面。

```text
┌─────────────────────────────────────┐
│ Session Title                       │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │          Slide Image            │ │
│ └─────────────────────────────────┘ │
│                                     │
│ 本文本文本文本文...                 │
│                                     │
│ 00:00–03:24                        │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │          Slide Image            │ │
│ └─────────────────────────────────┘ │
│                                     │
│ 本文本文本文...                     │
└─────────────────────────────────────┘
```

各sectionで可能:

- body編集
- OCR確認
- raw transcript確認
- corrected transcript確認
- representative image変更
- section再生成

---

# 38. Screen 5 — Export

Primary:

```text
[ Export HTML ]
```

Secondary:

```text
[ Markdown ]
[ JSON ]
[ TXT ]
[ SRT ]
```

Export previewを表示してもよい。

---

# 39. HTML Export

Export時:

```text
export-dir/
├─ index.html
└─ assets/
   ├─ slide-001.jpg
   ├─ slide-002.jpg
   └─ ...
```

オプション:

```ts
type HtmlExportOptions = {
  includeTimestamps: boolean;
  includeOcrDetails: boolean;
  includeRawTranscript: boolean;
  theme: "light";
};
```

v1ではlightのみでもよい。

---

# 40. Pipeline Interfaces

## Media

```ts
interface MediaEngine {
  probe(path: string): Promise<MediaMetadata>;

  extractFrame(input: {
    path: string;
    timestampMs: number;
    crop?: CropRegion;
  }): Promise<string>;

  extractAudio(input: {
    path: string;
    startMs: number;
    endMs: number;
  }): Promise<string>;
}
```

## Speech

```ts
interface SpeechEngine {
  transcribe(input: {
    mediaPath: string;
    startMs: number;
    endMs: number;
  }): Promise<RawTranscript>;
}
```

## OCR

```ts
interface OcrEngine {
  recognize(input: {
    imagePath: string;
  }): Promise<SlideOcrResult>;
}
```

## Correction

```ts
interface TranscriptCorrectionEngine {
  correct(input: {
    transcript: string;
    ocrText: string;
    terms: string[];
  }): Promise<CorrectedTranscript>;
}
```

## Article

```ts
interface ArticleFormatter {
  format(input: {
    correctedTranscript: string;
    ocrText: string;
  }): Promise<ArticleFormattingResult>;
}
```

## Export

```ts
interface ArticleExporter {
  exportHtml(
    article: VideoNotesArticle,
    destination: string,
    options: HtmlExportOptions,
  ): Promise<void>;

  exportMarkdown(
    article: VideoNotesArticle,
    destination: string,
  ): Promise<void>;
}
```

---

# 41. Pipeline API

```ts
type PipelineOptions = {
  crop: CropRegion;

  slideDetection: {
    sampleIntervalMs: number;
    threshold: number;
  };

  transcription: boolean;
  ocr: boolean;
  correction: boolean;
  articleFormatting: boolean;
};
```

```ts
interface VideoNotesPipeline {
  run(
    sourcePath: string,
    options: PipelineOptions,
  ): Promise<MediaProject>;
}
```

個別stageも再実行可能。

```ts
pipeline.detectSlides(projectId);
pipeline.transcribe(projectId);
pipeline.runOcr(projectId);
pipeline.correctTranscript(projectId);
pipeline.formatArticle(projectId);
pipeline.exportHtml(projectId);
```

---

# 42. Security / Privacy

必須:

- 動画を外部送信しない
- 音声を外部送信しない
- OCR結果を外部送信しない
- Transcriptを外部送信しない
- AI inferenceはlocalhostのみ
- Telemetryなしをデフォルト
- HTML exportにも外部trackingを入れない

Network利用は原則:

```text
sidecar/model download
```

のみ。

---

# 43. Error Handling

```ts
type AppErrorCode =
  | "FILE_NOT_FOUND"
  | "UNSUPPORTED_MEDIA"
  | "FFMPEG_FAILED"
  | "WHISPER_FAILED"
  | "LLAMA_SERVER_FAILED"
  | "MODEL_NOT_FOUND"
  | "MODEL_DOWNLOAD_FAILED"
  | "MODEL_CHECKSUM_FAILED"
  | "OUT_OF_MEMORY"
  | "PROCESS_CANCELLED"
  | "EXPORT_FAILED"
  | "INVALID_PROJECT";
```

UI向けmessageとdebug detailを分離する。

---

# 44. Performance Strategy

M1 16GB基準。

## Sequential Models

必須。

## OCR

```ts
const OCR_CONCURRENCY = 1;
```

## Whisper

複数process並列実行しない。

## Hash

縮小画像で実行。

## OCR対象

1スライド1代表画像を基本。

全フレームOCR禁止。

## Cache

再処理回避を優先。

---

# 45. Repository Structure

```text
video-notes/
├─ src/
│  ├─ app/
│  ├─ components/
│  ├─ features/
│  │  ├─ import/
│  │  ├─ crop/
│  │  ├─ slide-detection/
│  │  ├─ processing/
│  │  ├─ transcription/
│  │  ├─ ocr/
│  │  ├─ correction/
│  │  ├─ article/
│  │  └─ export/
│  │
│  ├─ lib/
│  │  ├─ ffmpeg/
│  │  ├─ whisper/
│  │  ├─ llama/
│  │  ├─ hashing/
│  │  ├─ pipeline/
│  │  ├─ models/
│  │  ├─ storage/
│  │  └─ export/
│  │
│  ├─ schemas/
│  └─ types/
│
├─ src-tauri/
│  ├─ src/
│  │  ├─ lib.rs
│  │  └─ commands/
│  ├─ binaries/
│  ├─ capabilities/
│  └─ tauri.conf.json
│
├─ scripts/
│  ├─ dev.ts
│  ├─ setup.ts
│  ├─ prepare-sidecars.ts
│  └─ prepare-models.ts
│
├─ package.json
├─ bun-lock.yaml
└─ README.md
```

---

# 46. Development Phases

## Phase 0 — Foundation

- Tauri 2
- React + TS
- Bun
- ffmpeg/ffprobe sidecar
- setup automation

完了:

```bash
git clone
bun install
bun run dev
```

で起動。

---

## Phase 1 — Import / Crop

- video import
- ffprobe
- preview
- crop UI
- frame extraction

---

## Phase 2 — Slide Detection

- frame sampling
- hash
- hamming distance
- boundary detection
- timeline
- manual correction

---

## Phase 3 — Whisper

- whisper.cpp
- model download
- audio extraction
- slide transcript

---

## Phase 4 — OCR

- llama-server
- OCR model
- representative frame
- slide OCR

---

## Phase 5 — Transcript Correction

- text LLM
- reference terms
- correction
- correction audit

---

## Phase 6 — Article Formatting

- corrected transcript → clean article body
- section editor
- regenerate section

---

## Phase 7 — HTML Export

- Article model
- local images
- self-contained CSS
- index.html
- Markdown export
- JSON export
- SRT export

---

## Phase 8 — v1 Polish

- Model Manager
- download progress
- model deletion
- resume projects
- error UX
- cache tuning
- settings
- packaged dmg

---

# 47. v0.1 Scope

最初の実用版:

```text
Import video
↓
Select slide crop
↓
Detect slide changes
↓
Manual boundary correction
↓
Whisper transcription
↓
Local OCR
↓
Local transcript correction
↓
Article formatting
↓
Review
↓
HTML export
```

ここまでをVideo Notesのコアとする。

---

# 48. Acceptance Criteria

## Functional

- M1 Mac 16GBで動作
- Homebrewによるffmpeg / llama.cpp / whisper.cpp install不要
- GUIから動画を読み込める
- Crop領域を指定できる
- スライド境界を自動検出できる
- 境界を手動修正できる
- 各スライドの代表画像を生成できる
- Whisper文字起こしが完全ローカルで動く
- OCRが完全ローカルで動く
- OCR情報を使ったTranscript correctionが完全ローカルで動く
- corrected transcriptから記事本文を生成できる
- raw transcriptを保持する
- correction履歴を保持する
- 記事レビュー画面で本文修正できる
- HTML記事をexportできる
- HTMLがネット接続なしで表示できる
- Markdown / JSON / TXT / SRTをexportできる
- モデル取得後は完全オフラインで全処理可能

## Performance

- AIモデルを逐次ロード
- OCR concurrencyは初期値1
- 全フレームOCRをしない
- hashは低解像度で実行
- cacheを利用

## Developer Experience

```bash
git clone <repo>
cd video-notes
bun install
bun run dev
```

を目標とし、native dependencyの手動installを要求しない。

---

# 49. Design Decisions

## Tauriを採用

理由:

- M1 16GBでUI runtimeのmemory overheadを抑えたい
- heavy processingはsidecar
- Node.jsをアプリruntimeに必須としない
- UI / orchestrationはTypeScript中心
- Rust layerを薄くできる
- 配布サイズを抑えやすい

## whisper.cpp + llama.cpp

### whisper.cpp

Speech-to-Text専用。

### llama.cpp

- OCR / VLM
- Transcript correction
- Article formatting

に利用する。

無理に1runtimeへ統一しない。

---

# 50. Future Extensions

- HTML theme selection
- dark mode export
- PDF export
- print optimized HTML
- video timestamp links
- source video embedding
- section title generation
- table of contents
- chapter grouping
- slide deduplication
- automatic crop detection
- speaker diarization
- folder batch processing
- CLI
- local REST API
- watch folder
- local search / embeddings
- RAG
- PDF input
- presentation file input
- Windows / Linux

---

# 51. 最終方針

Video Notesは文字起こしツールではなく、

**「動画を、スライド画像付きの記事へ変換する完全ローカルアプリ」**

として設計する。

```text
Conference / Lecture Video
↓
Slide Detection
↓
Speech Transcription
↓
Slide OCR
↓
Transcript Correction
↓
Article Formatting
↓
HTML Article
```

処理の責務:

```text
ffmpeg
→ media processing

whisper.cpp
→ speech transcription

llama.cpp
→ OCR
→ transcript correction
→ article formatting

Tauri + TypeScript
→ GUI
→ orchestration
→ review
→ HTML generation
→ export
```

最重要な完成形:

```text
動画を入力
↓
ローカル処理
↓
画像とテキストが交互に並ぶ読みやすい記事
↓
index.htmlとして持ち出せる
```

クラウドAPIを使わず、動画・音声・OCR・TranscriptをユーザーのMacから外へ出さない。
