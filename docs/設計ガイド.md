# Video Notes 設計ガイド

この文書は、[初期構想.md](./初期構想.md)に記載した処理パイプラインを、変更しやすいコード構造へ落とし込むためのガイドです。

Video Notesは、動画を入力として、スライド画像と発話内容が交互に並ぶ記事を生成する完全ローカルのデスクトップアプリです。画面、パイプライン、外部プロセス、保存データの責務を分離し、処理の一部だけを再実行できる構造を優先します。

## 1. 設計の基本方針

### 1.1 機能単位で分ける

画面の種類ではなく、ユーザーが行う作業単位でコードをまとめます。

```text
Import
Crop
Slide Detection
Processing
Transcription
OCR
Correction
Article
Export
```

各機能は、ページ、表示部品、状態管理、機能固有の型・純粋関数を同じディレクトリに置きます。

### 1.2 `App.tsx`は組み立て役にする

`App.tsx`に以下を書かないでください。

- ファイル選択の実装
- ffmpeg / ffprobeの呼び出し
- AIモデルとの通信
- 大きな画面レイアウト
- プロジェクトデータの変換ロジック

アプリの入口では、現在の画面やアプリ全体のProviderを組み立てるだけにします。

現在の入口は画面遷移とプロジェクト状態の組み立てだけを担当します。

```tsx
import { CropPage } from '../features/crop/CropPage'
import { ImportPage } from '../features/import/ImportPage'

function App() {
  // stepとMediaProjectを管理し、各Pageへ渡す
}
```

### 1.3 TypeScriptがオーケストレーション、RustがOS境界

仕様書の方針に合わせ、AIやパイプラインの判断はTypeScript側に置きます。

Rust側は次に限定します。

- Tauri command
- sidecarの起動・停止・権限
- ファイルシステム連携
- OS固有処理
- プロセスのライフサイクル

Rust側にOCR、文字起こし、プロンプト、記事整形のロジックを実装しません。

### 1.4 `project.json`を正規状態にする

画面上の一時状態と、保存・再開するプロジェクト状態を分けます。

```text
UIの一時状態
  例: ドラッグ中、ダイアログ表示中、選択中

MediaProject
  例: 動画パス、Crop、スライド境界、OCR、Transcript
```

保存対象の正規状態は`MediaProject`とし、`project.json`へ保存します。キャッシュは再生成可能な派生データとして扱います。

### 1.5 ステージを再実行可能にする

各処理ステージは、入力が同じなら同じ結果を再利用できるようにします。

```text
source + crop
  → frame / hash cache

boundaries
  → audio / transcript cache

representative image
  → OCR cache

transcript + OCR
  → correction cache

corrected transcript
  → article formatting cache
```

キャッシュには入力ハッシュとモデル情報を持たせ、モデルや入力が変わった場合だけ再処理します。

## 2. ディレクトリ構造

仕様書のRepository Structureをベースに、現在実装済みの場所と今後の配置を次のように統一します。

```text
video-notes/
├─ src/
│  ├─ app/
│  │  └─ App.tsx
│  │
│  ├─ components/
│  │  ├─ AppHeader.tsx
│  │  ├─ VideoPlaybackControls.tsx
│  │  └─ WorkflowBar.tsx
│  │
│  ├─ features/
│  │  ├─ import/
│  │  │  ├─ components/
│  │  │  │  ├─ SelectedVideoSummary.tsx
│  │  │  │  ├─ VideoDropZone.tsx
│  │  │  │  └─ VideoPreview.tsx
│  │  │  ├─ hooks/
│  │  │  │  └─ useVideoPicker.ts
│  │  │  ├─ ImportPage.tsx
│  │  │  ├─ types.ts
│  │  │  └─ utils.ts
│  │  │
│  │  ├─ crop/
│  │  │  ├─ components/CropSelector.tsx
│  │  │  ├─ CropPage.tsx
│  │  │  ├─ types.ts
│  │  │  └─ utils.ts
│  │  ├─ slide-detection/
│  │  │  ├─ components/SlideDetectionVideo.tsx
│  │  │  ├─ components/SlideDetectionTimeline.tsx
│  │  │  ├─ components/SlideDetectionSettingsStatus.tsx
│  │  │  ├─ components/SlideDetectionResultPanel.tsx
│  │  │  ├─ components/SlideSegmentList.tsx
│  │  │  ├─ components/SlideThumbnail.tsx
│  │  │  ├─ hooks/useSlideDetection.ts
│  │  │  ├─ SlideDetectionPage.tsx
│  │  │  ├─ detection.ts
│  │  │  ├─ types.ts
│  │  │  └─ utils.ts
│  │  ├─ processing/
│  │  ├─ transcription/
│  │  ├─ ocr/
│  │  ├─ correction/
│  │  ├─ article/
│  │  └─ export/
│  │     ├─ hooks/useExport.ts
│  │     ├─ ExportPage.tsx
│  │     ├─ export.ts
│  │     └─ renderers.ts
│  │
│  ├─ lib/
│  │  ├─ tauri/
│  │  │  ├─ dialog.ts
│  │  │  ├─ environment.ts
│  │  │  ├─ filesystem.ts
│  │  │  └─ sidecar.ts
│  │  ├─ media/
│  │  │  ├─ ffmpeg.ts
│  │  │  ├─ dhash.ts
│  │  │  └─ ffprobe.ts
│  │  ├─ hashing/
│  │  ├─ whisper/
│  │  ├─ llama/
│  │  ├─ pipeline/
│  │  ├─ models/
│  │  ├─ project/
│  │  ├─ storage/
│  │  │  ├─ projectAssets.ts
│  │  │  └─ projectStorage.ts
│  │  └─ export/
│  │
│  ├─ schemas/
│  │  └─ project.ts
│  ├─ types/
│  │  ├─ media.ts
│  │  └─ project.ts
│  ├─ index.css
│  └─ main.tsx
│
├─ src-tauri/
│  ├─ src/
│  │  ├─ commands/
│  │  ├─ lib.rs
│  │  └─ main.rs
│  ├─ binaries/
│  ├─ capabilities/
│  └─ tauri.conf.json
│
├─ scripts/
│  ├─ dev.ts
│  ├─ setup.ts
│  ├─ prepare-sidecars.ts
│  └─ prepare-models.ts
│
├─ docs/
│  ├─ 初期構想.md
│  └─ 設計ガイド.md
│
├─ package.json
└─ bun.lock
```

### 2.1 sidecarの取得と同梱

`src-tauri/tauri.conf.json`の`bundle.externalBin`に`binaries/ffmpeg`、`binaries/ffprobe`、`binaries/whisper-cli`、`binaries/llama-server`を登録しています。Tauriはビルド対象のtarget tripleを末尾につけたファイルをsidecarとしてbundleするため、macOS Apple Siliconでは次のファイルになります。

```text
src-tauri/binaries/ffmpeg-aarch64-apple-darwin
src-tauri/binaries/ffprobe-aarch64-apple-darwin
src-tauri/binaries/whisper-cli-aarch64-apple-darwin
src-tauri/binaries/llama-server-aarch64-apple-darwin
```

これらはリポジトリへ大きなバイナリを直接コミットせず、`bun run dev`または`bun run build`の前処理で`bun run prepare:sidecars`が不足時だけ取得します。取得後は固定リリースのSHA-256を検証し、実行権限を付与します。llama.cppは複数のdylibを必要とするため、`src-tauri/binaries/llama-runtime`へランタイムライブラリを展開し、開発時と`.app`内の双方から参照できるrpathを`llama-server`へ設定します。アプリ実行時の呼び出しは`src/lib/tauri/sidecar.ts`に閉じ込め、動画固有の変換は`src/lib/media`から利用します。

現在の取得元はffmpeg / ffprobeがmacOS Apple Silicon向け静的ビルドを提供するMartin Riedlのビルドサーバー、whisper-cliとllama-serverがmacOS Apple Silicon向けビルドを提供する固定GitHub Releaseです。いずれも`scripts/prepare-sidecars.ts`でURLとSHA-256を固定しています。リリースを更新するときは、同ファイルのURL・リリース番号・SHA-256を同時に更新し、ライセンスと再配布条件も確認します。

空の機能ディレクトリを最初から大量に作る必要はありません。機能を実装するタイミングで、その機能のディレクトリとファイルを追加します。

## 3. 各ディレクトリの責務

### `src/app`

アプリ全体の起動点と画面の組み立てを置きます。

- `App.tsx`
- 将来のProvider
- 将来の画面遷移・ルート選択

機能の詳細には依存しますが、Tauriプラグインを直接呼び出しません。

### `src/components`

複数の機能・画面から使うUI部品を置きます。

現在は、ヘッダーとワークフローステップをここに置いています。

ルール:

- ビジネスロジックを持たない
- `features/*`へ依存しない
- Tauri APIを直接呼び出さない
- 汎用化できない機能固有部品は各featureへ置く

### スタイリング

Tailwind CSS v4を標準とし、Viteプラグインで読み込みます。

- 画面・部品のスタイルはTailwindのutility classを`className`へ書く
- `src/index.css`はTailwindの読み込みを基本とする
- 機能ごとのCSSファイルは新設しない
- 同じスタイルが繰り返される場合は、まずUIコンポーネント化を検討する
- Tailwindで表現できない外部ライブラリ固有の調整だけ、例外的にCSSを追加する

### `src/features`

ユーザー向けの機能を置きます。各featureは小さなアプリケーションとして扱います。

標準形:

```text
features/<feature>/
├─ components/
├─ hooks/
├─ <Feature>Page.tsx
├─ types.ts
└─ utils.ts または lib/
```

### `src/lib`

Reactから独立した処理と、外部システムとの接続を置きます。

#### `lib/tauri`

TauriプラグインやTauri commandの薄いラッパーです。

例:

```ts
export async function pickVideoPath(): Promise<string | null>
```

featureから`@tauri-apps/plugin-*`を直接importせず、可能な限りこの層を経由します。これによりブラウザ上のテストとTauri実装を分離できます。

#### `lib/media`

- ffprobe呼び出し
- ffmpeg呼び出し
- フレーム抽出
- 音声抽出
- 動画メタデータの変換

#### `lib/hashing`

- dHash
- pHash
- Hamming distance
- 入力ハッシュ

#### `lib/models`

- モデルの保存先
- 初回ダウンロード
- 途中ファイルの破棄
- SHA-256検証
- ダウンロード進捗

モデル本体はリポジトリへ含めず、`AppLocalData/models`へ保存します。モデルの取得は処理開始時に必要なものだけ行い、完了後は同じモデルを再利用します。

#### `lib/whisper` / `lib/llama`

- sidecarまたはlocalhost serverの起動
- healthcheck
- リクエスト送信
- 結果のparse
- 終了処理

プロンプトの組み立ては`lib/llama`または利用するfeatureの責務とし、Rustには置きません。

#### `lib/pipeline`

ステージの順序、進捗、キャンセル、再開、キャッシュ判定を管理します。

### `src/types`

複数featureから参照するドメイン型を置きます。

将来的には次の型をここへ移します。

- `MediaProject`
- `MediaSource`
- `MediaMetadata`
- `CropRegion`
- `SlideData`
- `SlideBoundary`
- `TranscriptSegment`
- `Correction`
- `ArticleSection`
- `VideoNotesArticle`
- `ProcessingJob`

ReactのProps専用型や、そのfeatureだけで完結する一時状態は各featureの`types.ts`に置きます。

### `src/schemas`

Zodによる実行時検証を置きます。

- `projectSchema`
- `mediaMetadataSchema`
- `slideSchema`
- `articleSchema`
- `jobSchema`

JSONを読み込むときは、型アサーションだけで済ませず、必ずschemaで検証します。

### `src-tauri`

Tauri固有のOS境界です。

```text
React / TypeScript
  ↓ lib/tauri
Tauri command / plugin
  ↓
filesystem / sidecar / OS
```

sidecarの引数やプロセス管理に必要な処理はRustへ置けますが、アプリケーションの業務ルールはTypeScript側に残します。

## 4. 依存方向

依存方向は次を基本とします。

```text
app
 ├─ features
 └─ components

features
 ├─ components
 ├─ lib
 ├─ types
 └─ schemas

components
 └─ 共通の型・スタイルのみ

lib
 ├─ types
 └─ schemas

types
 └─ 他のUI層・Tauri層へ依存しない
```

避ける依存:

- `components` → `features`
- `types` → React / Tauri
- `lib` → Reactコンポーネント
- `features` → `src-tauri`の内部ファイル
- feature間の直接的な循環依存

別featureの機能が必要になった場合は、共通の`lib`、`types`、`schemas`へ責務を移します。

## 5. Import機能の現在の設計

現在のImport機能は、次のように分割されています。

```text
ImportPage
 ├─ AppHeader
 ├─ WorkflowBar
 ├─ VideoDropZone
 ├─ SelectedVideoSummary
 └─ import footer action

useVideoPicker
 ├─ Tauri native dialog
 ├─ drag & drop
 └─ extension validation

lib/tauri/dialog
 └─ @tauri-apps/plugin-dialog の薄いラッパー
```

`ImportPage`はレイアウトの組み立てに集中し、ファイル選択の非同期処理は`useVideoPicker`へ分離しています。

Importでは、選択した動画のファイル名・拡張子・サイズに加えて、sidecarのffprobeで長さ・解像度・FPSを取得します。HTML videoの`canplay`とffprobeの両方が成功した場合だけREADYとし、壊れたファイルや拡張子だけを書き換えたファイルはCROPへ進めません。

動画のメタデータは`MediaProject.source.metadata`に保持し、プロジェクトはTauriの`AppLocalData/projects/<project-id>/project.json`へ保存します。動画本体はコピーせず、選択元の絶対パスだけを参照します。

Cropを保存しても、Crop済みの動画ファイルは作成しません。Slide Detectionでは元動画へffmpegの`crop` filterを適用し、9×8のグレースケールフレームだけを一時的に読み取ります。境界情報はtimestampとしてProjectへ保存し、必要になった段階でだけ部分処理を行います。

## 6. 状態管理

### 6.1 画面内の一時状態

次のような状態はfeature内のhookで管理します。

- ダイアログを開いているか
- ドラッグ中か
- 選択中か
- 入力エラー
- 選択したファイルの表示状態

### 6.2 プロジェクト状態

複数画面から参照する状態は`MediaProject`を中心に扱います。

初期方針は次のとおりです。

- 小さな画面内状態: `useState`
- 複数イベントを持つfeature: `useReducer`
- 複数featureで共有する現在のproject: React Context + reducer
- 外部state manager: 必要性が明確になるまで導入しない

同じ値を各画面で別々に持たず、保存対象の値はProject stateに集約します。

## 7. パイプライン設計

処理ステージは次の順番です。

```text
probe
  → detect-slides
  → extract-audio
  → transcribe
  → ocr
  → correct
  → format-article
  → export
```

各stageは次の性質を持たせます。

- 入力と出力を型で定義する
- 進捗をJob Managerへ通知する
- キャンセル可能にする
- 失敗理由を`AppErrorCode`へ変換する
- 入力ハッシュが一致すればcacheを使う
- stage単位で再実行できる

AIモデルは同時に常駐させません。

```text
Whisper load → transcription全部 → unload
OCR load → OCR全部 → unload
Text LLM load → correction / formatting → unload
```

## 8. エラーと境界

ユーザー向けメッセージとデバッグ情報を分けます。

```ts
type AppError = {
  code: AppErrorCode
  message: string
  detail?: string
  cause?: unknown
}
```

例えば、UIには「動画を読み込めませんでした」と表示し、ログにはffprobeの終了コードやstderrを残します。

外部通信はモデル・sidecarの取得時だけ許可し、アプリの処理中は動画、音声、OCR結果、Transcriptを外へ送信しません。

## 9. 実装ルール

- 1ファイルの責務を1つにする
- ページコンポーネントは部品を組み合わせる
- 非同期処理と状態遷移はhookまたはlibへ分ける
- `utils.ts`を無制限の寄せ集めにしない
- Tauri APIは`src/lib/tauri`に閉じ込める
- ドメイン型はUI型と分ける
- `project.json`の読み書きにはZod検証を通す
- AI結果はraw結果を破棄せず、補正前後を保存する
- 外部URL、CDN、外部フォントを成果物へ持ち込まない
- 変更後は`bun run build`と`bun run lint`を実行する

## 10. 実装ロードマップ

### 現在完了

- Tauri 2 + React + TypeScript + Bun
- ViteとTauriの開発ポート統一
- Dialogプラグイン
- Import画面
- Import画面のfeature分割
- Tauri APIラッパーの導入
- ffmpeg / ffprobe sidecarの自動取得・SHA-256検証・bundle設定
- ffprobeによる動画メタデータ表示
- ImportからCROPへの画面遷移
- Crop矩形の移動・四隅リサイズ・リセット・全画面指定
- Crop動画の再生・シーク操作を独立バーへ分離
- `project.json`へのプロジェクト状態保存とZod検証
- Crop保存後のSlide Detection画面への遷移
- ffmpeg sidecarによるCrop範囲の低解像度フレームサンプリング
- dHash・Hamming distance・輝度差によるスライド境界の自動検出
- 検出結果のタイムライン表示
- 境界のドラッグ移動・ダブルクリック追加・削除・しきい値変更・修正保存
- しきい値・サンプリング間隔を設定してから検出を手動開始
- 各Slideの代表フレームをJPEGとしてProject assetへ保存・目視確認
- 検出画面の動画プレイヤーとSlide区間の再生位置連動
- 再生位置に合わせたSlide一覧の自動追従とオン／オフ切り替え
- 検出画面の動画直下に検出タイムラインを配置し、赤い再生位置と動画を同期
- Generate Notes画面への遷移
- ffmpegによる16kHz / mono WAVの抽出とプロジェクト単位の音声キャッシュ
- whisper.cpp CLI sidecarの取得・同梱設定
- Whisperモデルの初回ダウンロード、SHA-256検証、ローカル保存
- 動画全体のタイムスタンプ付き文字起こしとSlide区間への割り当て
- Slide画像とraw transcriptを並べた文字起こし結果の確認画面
- llama.cpp `llama-server` sidecarとMetalランタイムの自動取得・同梱設定
- GLM-OCRモデルとmmprojモデルの初回ダウンロード・SHA-256検証
- 各Slideの代表画像に対する逐次OCR
- Slide単位のOCR結果保存、入力fingerprintによる再実行スキップ、途中結果の再利用
- 発話とスライド内文字を並べた解析結果の確認画面
- llama-serverのテキストモデル起動とOpenAI互換chat clientの共通化
- Qwen3 1.7B Q8_0モデルの初回ダウンロード・SHA-256検証
- 補正と記事整形で共有するText LLMモデル管理
- raw transcriptとSlide OCRを使ったSlide単位の文字起こし補正
- 補正結果・補正箇所の保存、入力fingerprintによる再実行スキップ
- 補正済み発話とraw発話の切り替え、補正箇所の確認画面
- 補正済み発話のSlide単位記事整形、途中結果保存、入力fingerprintによる再実行スキップ
- スライド画像と記事本文を並べた記事プレビュー
- 記事タイトル・Slide本文の編集、一括保存、元データの確認
- 記事レビューからExport画面への遷移
- HTML / Markdown / JSON / TXT / SRTの選択式Export
- HTML / Markdownの相対画像パスと`assets/`への代表画像コピー
- Export先フォルダの選択、進捗表示、完了・エラー表示

### 次に実装するもの

1. Slide単位の記事本文再生成
2. 保存済みプロジェクトの一覧・再開

### その後

```text
Crop
 → Slide Detection
 → Boundary Review
 → Whisper
 → OCR
 → Transcript Correction
 → Article Formatting
 → Export
```

各機能の実装開始時に、対応するfeatureディレクトリを作成し、処理本体は`lib`、永続データは`types`と`schemas`へ置きます。
